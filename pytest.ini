[pytest]
testpaths = tests
python_files = test_*.py
# Tests assume the working directory is the project root (data/ and
# src/mf_model.pkl paths are relative), matching how train.py and
# src/api.py are already run.
