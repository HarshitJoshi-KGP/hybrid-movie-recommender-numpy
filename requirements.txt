# Core
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
pyarrow>=14.0        # parquet read/write for data/preprocess.py

# Serving
fastapi>=0.104
uvicorn[standard]>=0.24
pydantic>=2.0
streamlit>=1.28
requests>=2.31

# Training artifacts
matplotlib>=3.7

# Lower bounds only (not exact pins): this is a portfolio/demo project, not
# a production service with a lockfile -- `>=` keeps it installable across
# Python 3.9-3.12 without fighting resolver conflicts. For a real deployment,
# freeze exact versions with `pip freeze > requirements.lock.txt`.
